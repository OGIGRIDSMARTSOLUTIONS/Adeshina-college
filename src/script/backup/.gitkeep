# Keep this folder in Git, but ignore backup files inside it (.gitignore).
# When you run a sync command, the previous src/data/*.ts file is copied here
# as e.g. programmes.ts.20260904-003015.bak — local safety only, not for GitHub.
